{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Digideck FE",
  "repository": "https://github.com/parugupta/DigideckFE",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "update-deps": "npm update",
    "postupdate-deps": "bower update",
    "pretest": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
